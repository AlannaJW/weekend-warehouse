version: 2

sources:
  - name: raw
    database: weekend-warehouse
    schema: raw
    tables:
      - name: raw_customers
      - name: raw_orders
      - name: raw_tier_history
      - name: raw_web_events
